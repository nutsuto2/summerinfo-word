apiVersion: v1
kind: PersistentVolume
metadata:
  name: auth-postgres-volume
  labels:
    type: local
    app: auth-postgres
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/postgresql