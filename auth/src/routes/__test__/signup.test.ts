import request from 'supertest';
import { app } from '../../app';

// returns validation error when do not enter email

// returns validation error when email is incorrect

// returns validation error when do not enter username

// returns validation error when username is incorrect

// returns validation error when do not enter password

// returns validation error when password is incorrect

// returns validation error when the email and username is already existed

